{
  "private": true,
  "scripts": {
    "build": "ls -la && yarn run build:css && trunk build",
    "dev": "yarn run build:css && trunk serve",
    "build:css": "postcss src/styles/tailwind.css -o static/dist/tailwind.css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.8",
    "postcss": "^8.4.16",
    "postcss-cli": "^8.3.1",
    "postcss-import": "^14.0.1",
    "postcss-loader": "^5.2.0",
    "tailwindcss": "^3.1.8"
  }
}
